# 任务05b：纠正后的严格映射 K5M3 一跳冻结

## 目的

正确合并任务02的严格实体映射与任务04确定的统一 K=5、M=3，重新生成可作为后续方法共同起点的一跳 iKnow 基线。

## 冻结配置

- 六集统一 Top-K=5、Top-M=3。
- 逐数据集关系集合保持任务01/02不变。
- 直接知识文本 `class_name, tail`。
- 一跳联合、证据数归一化 LSE，scale=100。
- runner 必须读取根目录 `entity_mapping_v2.json`。
- 未映射类别不查询KG、不删除样本，保留原CLAP分数。
- 禁止首词/尾词回退。

本任务只输出 CLAP 与 iKnow-1st，不含动态 alpha、二跳、门控或LLM。
